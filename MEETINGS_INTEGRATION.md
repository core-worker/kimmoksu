# 회의록 적용 순서

## 1. Firebase 규칙 교체

Firebase 콘솔 → 해당 ERP 프로젝트 → Firestore Database → 규칙을 엽니다.

현재 규칙을 별도로 복사해 보관한 뒤 `firestore/firestore.rules`의 전체 내용으로 교체하고 **게시**합니다. `meetings.rules.fragment`는 이전 참고 조각입니다. 실제 교체에는 반드시 `firestore.rules` 전체를 사용하세요. 이전의 `match /{document=**}` 전체 허용 코드를 남겨 두지 마세요.

규칙 변경은 데이터를 삭제하지 않습니다. 이 단계와 코드 반영 사이에는 기존 직원은 계속 이용할 수 있지만, 기존 화면의 신규 팀 가입은 차단됩니다. 다음 코드 반영을 같은 작업 시간에 완료하세요.

## 2. ERP 코드 반영

PR #2의 변경 내용을 main에 반영하고 기존 GitHub Pages 배포 완료 후 새로고침합니다. 운영 규칙 게시와 실제 로그인 계정 확인은 사용자가 Firebase 콘솔에서 진행해야 합니다.

## 3. 회의록 담당자 지정

관리자로 로그인 → 회의록 → 담당자 설정 → 작성 담당 직원을 체크합니다. 팀 소유자와 관리자는 기본적으로 작성할 수 있습니다. 담당자 지정 후 해당 직원은 새로고침합니다.

담당자 계정으로 임시저장·게시하고, 일반 직원 계정으로 게시글은 보이고 임시저장은 보이지 않는지 확인합니다. 검색과 수정, 모바일 메뉴도 확인합니다.

## 4. 새 직원 가입

새 직원이 기존처럼 초대코드를 입력하면 즉시 합류하는 대신 가입 요청이 접수됩니다. 관리자/소유자가 **팀원 및 조직 관리 → 팀 가입 요청 → 승인**하면 팀원이 됩니다. 요청자는 승인 후 다시 로그인합니다.

이 변경은 자기 자신을 팀원 목록에 추가해 회의록 권한을 얻는 경로를 막기 위해 필요합니다. 기존 팀원의 소속은 변경하지 않습니다.

## 규칙 적용 범위

- 회의록 게시글: 해당 팀원만 읽기, 관리자·지정 담당자만 작성/수정.
- 초안: 작성 권한이 있는 본인만 읽고 쓰기. 관리자도 타인의 초안을 읽지 않음.
- 팀 권한: 본인 관리자 임명, 담당자 지정, 자기 가입, 타인 소유권 변경 차단.
- 기존 ERP의 다른 컬렉션과 회의록 외 팀 하위 컬렉션은 기존 로그인 사용자 허용 정책을 유지합니다. **전체 ERP 보안 개선을 완료한 규칙이 아닙니다.**
- 기존 초대코드 검색을 위해 팀 문서의 메타데이터 읽기는 로그인 사용자에게 유지합니다. 초대코드는 이제 가입 요청에만 사용되며 자동 가입 권한을 주지 않습니다.

## 구현과 검증

회의록 화면·기능은 승인된 테스트 버전을 사용합니다. 실제 Firebase 로그인/팀을 사용하고 테스트 데이터·역할 전환 UI는 제외했습니다. 서버 타임스탬프, 수정 revision, 트랜잭션 게시, 계정별 임시 복구를 적용했습니다. 새 메뉴로 기존 홈 바로가기가 어긋나지 않도록 이름 기반 연결을 사용합니다.

- 클라이언트 검증: `node tests/meetings.test.cjs`
- 규칙 검증: firebase-tools, firebase, @firebase/rules-unit-testing 설치 후 `firebase emulators:exec --only firestore --project demo-kimmoksu-meetings --config firebase.meetings-test.json "node tests/meetings.rules.test.cjs"`

에뮬레이터는 실제 운영 데이터에 접근하지 않는 demo 프로젝트를 사용합니다. 검증 결과는 PR에 기록합니다. 실제 콘솔 게시와 직원 계정 실환경 확인은 별도입니다.
