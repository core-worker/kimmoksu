# 회의록 ERP 적용

테스트에서 확정된 화면과 작성·임시저장·게시·검색·상단 고정 기능을 ERP에 통합했습니다.

- 기존 로그인 사용자와 팀을 사용합니다. 계정/모드 전환용 테스트 UI와 예시 데이터는 포함하지 않습니다.
- `teams/{teamId}/meetings`에는 게시된 회의록만 저장합니다.
- `teams/{teamId}/meetingDrafts/{uid}/items`에는 작성자별 초안을 저장합니다. 다른 담당자/관리자도 타인의 초안을 읽지 않습니다.
- 팀 소유자·관리자와 `meetingAdmins` 담당자가 작성/수정합니다. 기존 ERP의 최고권한 계정 예외도 팀원일 때만 동일하게 적용합니다.
- 홈에는 최근 게시글 3개, 목록에는 고정된 글 우선/최신순으로 표시합니다.
- 기존 테마와 모바일 메뉴를 사용합니다. 회의록 CSS/DOM/이벤트는 별도로 구분합니다.
- 메뉴 추가로 기존 홈 바로가기가 어긋나지 않도록 숫자 인덱스 대신 페이지 이름을 사용합니다.
- 편집 중에도 게시글 원문은 유지되고, 다시 게시할 때만 원문과 초안을 트랜잭션으로 처리합니다. 동시 편집은 revision으로 감지합니다.
- 임시저장 실패 시 브라우저에 계정·팀별 복구본을 남기고 성공으로 표시하지 않습니다. 회의록 열람 여부는 계정·팀·브라우저별로 저장됩니다.

## 운영 적용 전 필수 확인

현재 저장소에는 배포된 Firestore 보안 규칙과 Firebase 관리/배포 설정이 없습니다. 따라서 이 PR만 병합하면 서버 권한이 적용되었다고 볼 수 없습니다.

1. Firebase 콘솔에서 실제 사용 중인 규칙 전체를 확보하고 백업합니다.
2. `firestore/meetings.rules.fragment`를 기존 documents 블록 안에 통합합니다. 파일 전체를 기존 규칙 위에 덮어쓰지 않습니다.
3. 기존 `teams` 쓰기 조건에 meetingAdmins 변경 제한을 AND로 적용합니다. 소유자·관리자·members 등의 기존 권한 상승 경로도 확인합니다. 다른 match의 광범위한 allow는 OR로 우회될 수 있으므로 함께 점검합니다.
4. 에뮬레이터/별도 Firebase 테스트 환경에서 비로그인, 다른 팀, 일반 직원 쓰기, 타인 초안 읽기, 담당자 지정/해제, 본문 위변조·과도한 크기, revision 충돌을 검증합니다. 규칙은 현재 실제 환경에서 컴파일/배포 검증되지 않았습니다.
5. 규칙 확인 후 코드 반영 및 실제 직원 계정 2개로 게시·열람·권한 테스트를 진행합니다.

**이 PR은 위 서버 규칙 검증 전에는 운영 배포하지 않는 초안입니다.** 기존 프로젝트의 데이터와 운행기록부 브랜치는 변경하지 않습니다.

## 개발 검증

`node tests/meetings.test.cjs`

테스트는 Firestore를 대체한 실행 환경에서 클라이언트의 게시/초안 분리, 검색, 저장 실패, 계정 종료와 트랜잭션 충돌 처리를 확인합니다. 실제 Firebase 규칙/네트워크/브라우저 레이아웃 검증을 대체하지 않습니다.
